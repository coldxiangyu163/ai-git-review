'use strict';

/**
 * Format the review result for terminal output.
 * @param {object} review - The raw review result from LLM.
 * @returns {string} Formatted review string.
 */
function formatReview(review) {
  // TODO: implement
}

module.exports = { formatReview };
